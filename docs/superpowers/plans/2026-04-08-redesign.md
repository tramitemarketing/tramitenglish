# TrainEnglish UI Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Redesign all 11 pages of TrainEnglish with Dark Editorial / Amber style, dynamic background (grid + particles + glow blobs), animated progress bar, and a per-page theme toggle — without changing any functionality.

**Architecture:** All visual changes go through `css/style.css` (new animation/background system) + per-page HTML additions (background div, particles JS, theme toggle button). Functionality is untouched. diagnostic.js contracts (IDs/classes) are preserved.

**Tech Stack:** Vanilla HTML/CSS/JS, no build step, no new dependencies.

---

## File Map

| File | Change |
|------|--------|
| `css/style.css` | Add: grid bg, glow blobs, particles, animations, enhanced card/btn hover, theme toggle topbar icon, body fade-in |
| `index.html` | Add: page-bg divs, particles JS, theme toggle in topbar |
| `dashboard.html` | Add: page-bg, particles, day-progress bar, count-up on stats, theme toggle |
| `session.html` | Add: page-bg, particles, theme toggle |
| `test.html` | Add: page-bg, particles, theme toggle, card hover lift |
| `diagnostic.html` | Add: page-bg, particles, theme toggle — preserve all JS contracts |
| `library.html` | Add: page-bg, particles, theme toggle |
| `stats.html` | Add: page-bg, particles, theme toggle, count-up on metric cards |
| `settings.html` | Add: page-bg, particles — theme toggle already exists |
| `log.html` | Add: page-bg, particles, theme toggle |
| `review.html` | Add: page-bg, particles, theme toggle |

---

## Shared Snippets (reference for all tasks)

### A. Page background HTML (add right after `<body>` tag, before topbar)
```html
<!-- Dynamic background -->
<div class="page-bg" aria-hidden="true"></div>
<div class="page-glow page-glow-1" aria-hidden="true"></div>
<div class="page-glow page-glow-2" aria-hidden="true"></div>
```

### B. Particles JS (add as last `<script>` before `</body>`)
```html
<script>
(function () {
  var pts = [
    {l:'10%',t:'22%',s:2,d:0,o:0.5},
    {l:'35%',t:'48%',s:1.5,d:0.7,o:0.3},
    {l:'60%',t:'15%',s:3,d:1.4,o:0.4},
    {l:'80%',t:'58%',s:2,d:0.3,o:0.35},
    {l:'22%',t:'72%',s:1.5,d:1.9,o:0.25},
    {l:'90%',t:'33%',s:2.5,d:1,o:0.3}
  ];
  var f = document.createDocumentFragment();
  pts.forEach(function (p) {
    var el = document.createElement('div');
    el.className = 'particle';
    el.setAttribute('aria-hidden', 'true');
    el.style.cssText = 'left:'+p.l+';top:'+p.t+';width:'+p.s+'px;height:'+p.s+'px;--op:'+p.o+';animation:float-up '+(3+p.d)+'s ease-in-out '+p.d+'s infinite;';
    f.appendChild(el);
  });
  document.body.appendChild(f);
})();
</script>
```

### C. Theme toggle button (replace existing settings icon in topbar)
Replace the gear icon `&#9881;` settings link in dashboard/session/test/library/stats/log/review pages with:
```html
<div style="display:flex;align-items:center;gap:4px;">
  <button class="topbar-icon" id="themeBtn" aria-label="Cambia tema"></button>
  <a href="settings.html" class="topbar-icon" aria-label="Impostazioni">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
  </a>
</div>
```

### D. Theme toggle JS (add in page script, after other init code)
```js
// Theme toggle
(function () {
  var SUN = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/></svg>';
  var MOON = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>';
  function isDark() {
    var t = localStorage.getItem('te_theme') || 'auto';
    if (t === 'dark') return true;
    if (t === 'light') return false;
    return window.matchMedia('(prefers-color-scheme:dark)').matches;
  }
  var btn = document.getElementById('themeBtn');
  if (!btn) return;
  btn.innerHTML = isDark() ? SUN : MOON;
  btn.addEventListener('click', function () {
    var next = isDark() ? 'light' : 'dark';
    localStorage.setItem('te_theme', next);
    if (next === 'dark') document.documentElement.setAttribute('data-theme', 'dark');
    else document.documentElement.setAttribute('data-theme', 'light');
    btn.innerHTML = isDark() ? SUN : MOON;
  });
})();
```

### E. Count-up JS helper (for stat numbers)
```js
function countUp(el, target, suffix, duration) {
  var start = 0;
  var step = target / (duration / 16);
  var current = start;
  var raf = requestAnimationFrame(function tick() {
    current = Math.min(current + step, target);
    el.textContent = Math.round(current) + (suffix || '');
    if (current < target) raf = requestAnimationFrame(tick);
  });
}
```

---

## Task 1: CSS — Dynamic Background System + Animations

**Files:** Modify `css/style.css`

- [ ] **Step 1: Add page-bg, glow blob, particle CSS** — append to end of `css/style.css`:

```css
/* ══ Dynamic Background System ══════════════════════════ */

/* Fixed background layer */
.page-bg {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 0;
  overflow: hidden;
}

/* Amber grid lines */
.page-bg::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(245,158,11,0.05) 1px, transparent 1px),
    linear-gradient(90deg, rgba(245,158,11,0.05) 1px, transparent 1px);
  background-size: 32px 32px;
  mask-image: linear-gradient(to bottom, transparent 0%, #000 25%, #000 65%, transparent 100%);
  -webkit-mask-image: linear-gradient(to bottom, transparent 0%, #000 25%, #000 65%, transparent 100%);
}

[data-theme="light"] .page-bg::before,
:root:not([data-theme="dark"]) .page-bg::before {
  background-image:
    linear-gradient(rgba(180,83,9,0.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(180,83,9,0.06) 1px, transparent 1px);
  background-size: 32px 32px;
}

@media (prefers-color-scheme: light) {
  :root:not([data-theme="dark"]) .page-bg::before {
    background-image:
      linear-gradient(rgba(180,83,9,0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(180,83,9,0.06) 1px, transparent 1px);
    background-size: 32px 32px;
  }
}

/* Glow blobs */
.page-glow {
  position: fixed;
  border-radius: 50%;
  filter: blur(70px);
  pointer-events: none;
  z-index: 0;
  animation: breathe-blob 5s ease-in-out infinite;
}

.page-glow-1 {
  width: 360px;
  height: 360px;
  background: rgba(245,158,11,0.07);
  top: -120px;
  right: -80px;
}

.page-glow-2 {
  width: 240px;
  height: 240px;
  background: rgba(245,158,11,0.05);
  bottom: 80px;
  left: -80px;
  animation-delay: 2.5s;
  animation-duration: 7s;
}

[data-theme="light"] .page-glow-1,
[data-theme="light"] .page-glow-2 {
  background: rgba(180,83,9,0.05);
}

@keyframes breathe-blob {
  0%, 100% { transform: scale(1);    opacity: 1; }
  50%       { transform: scale(1.18); opacity: 0.65; }
}

/* Floating particles */
.particle {
  position: fixed;
  border-radius: 50%;
  background: var(--accent);
  pointer-events: none;
  z-index: 0;
  opacity: var(--op, 0.4);
}

@keyframes float-up {
  0%, 100% { transform: translateY(0);    opacity: var(--op, 0.4); }
  50%       { transform: translateY(-10px); opacity: calc(var(--op, 0.4) * 1.8); }
}

/* Day progress bar */
.day-progress {
  height: 2px;
  background: var(--border);
  border-radius: 1px;
  overflow: hidden;
  margin-bottom: 14px;
  position: relative;
  z-index: 1;
}

.day-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent), #fbbf24);
  border-radius: 1px;
  width: 0%;
  transition: width 1.2s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ══ Enhanced interactions ═════════════════════════════ */

/* Card hover lift */
.card.interactive {
  transition: border-color 0.15s, transform 0.18s ease, box-shadow 0.18s ease;
}

.card.interactive:hover {
  border-color: var(--accent) !important;
  transform: translateY(-2px);
  box-shadow: 0 8px 24px var(--accent-shadow);
}

/* Primary button glow */
.btn-primary {
  transition: opacity 0.15s, transform 0.15s ease, box-shadow 0.15s ease !important;
}

.btn-primary:hover:not(:disabled) {
  opacity: 1 !important;
  transform: scale(1.02);
  box-shadow: 0 4px 20px var(--accent-shadow);
}

.btn-primary:active:not(:disabled) {
  transform: scale(0.98);
}

/* Page fade-in */
@keyframes page-fade {
  from { opacity: 0; transform: translateY(4px); }
  to   { opacity: 1; transform: translateY(0); }
}

body {
  animation: page-fade 0.2s ease-out;
}

/* Count-up entrance */
@keyframes count-in {
  from { opacity: 0; transform: translateY(5px); }
  to   { opacity: 1; transform: translateY(0); }
}

.count-in {
  animation: count-in 0.3s ease-out forwards;
}

/* Staggered card entrance */
@keyframes card-in {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

.card-animate {
  opacity: 0;
  animation: card-in 0.3s ease-out forwards;
}

/* Ensure content is above background */
.topbar, .bottom-nav, .page-content, .session-topbar,
main, header, footer, .settings-content, .stats-page {
  position: relative;
  z-index: 1;
}
```

- [ ] **Step 2: Verify dark/light grid colors** — open `index.html` via `python3 -m http.server 8080`, check that the grid is visible in dark mode and subtly visible in light mode. Grid should not be distracting.

- [ ] **Step 3: Commit**
```bash
cd "C:/Users/PC/Desktop/files/claude/tramitenglish"
git add css/style.css
git commit -m "feat: add dynamic background system — grid, particles, glow blobs, animations"
```

---

## Task 2: index.html + dashboard.html

**Files:** Modify `index.html`, `dashboard.html`

- [ ] **Step 1: Update index.html**

In `index.html`, after `<body>`, add snippet A (page-bg). In the `<style>` block, change the login card shadow to use amber glow:
```css
.login-card {
  box-shadow: 0 4px 40px rgba(180, 83, 9, 0.10), 0 1px 4px rgba(0,0,0,0.08);
}
```
Add the theme toggle button as a fixed element at top-right:
```html
<button class="topbar-icon" id="themeBtn" aria-label="Cambia tema" style="position:fixed;top:16px;right:16px;z-index:10;background:var(--surface);border:1px solid var(--border);"></button>
```
Add snippet B (particles JS) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 2: Update dashboard.html**

**2a.** After `<body>`, add snippet A (page-bg divs).

**2b.** In the topbar, replace the gear icon link:
```html
<!-- BEFORE -->
<a href="settings.html" class="topbar-icon" aria-label="Impostazioni">&#9881;</a>
<!-- AFTER -->
<div style="display:flex;align-items:center;gap:4px;">
  <button class="topbar-icon" id="themeBtn" aria-label="Cambia tema"></button>
  <a href="settings.html" class="topbar-icon" aria-label="Impostazioni">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
  </a>
</div>
```

**2c.** Add day progress bar inside `.page-content`, above `.dash-grid` (after the `<main>` tag):
```html
<div class="day-progress" aria-label="Progresso piano" role="progressbar">
  <div class="day-progress-fill" id="dayProgressFill"></div>
</div>
```

**2d.** Add `card-animate` class to each `.card` in dashboard and set animation delays inline:
```html
<div class="card card-animate" style="animation-delay:0.05s">...</div>
<div class="card card-animate" style="animation-delay:0.1s">...</div>
<div class="card card-animate" style="animation-delay:0.15s">...</div>
```

**2e.** Add `interactive` class to the CTA card (the "Sessione di oggi" card).

**2f.** In the existing `<script>` block at the bottom of dashboard.html, add inside the IIFE after stats are rendered:
```js
// Day progress bar
var dayFill = document.getElementById('dayProgressFill');
if (dayFill) {
  requestAnimationFrame(function() {
    dayFill.style.width = ((state.currentDay || 1) / 60 * 100).toFixed(1) + '%';
  });
}
// Count-up on stats
function countUp(el, target, suffix) {
  if (!el || isNaN(target)) return;
  var step = target / 20;
  var cur = 0;
  (function tick() {
    cur = Math.min(cur + step, target);
    el.textContent = Math.round(cur) + (suffix || '');
    if (cur < target) requestAnimationFrame(tick);
  })();
}
// Apply to existing stat elements after they're set
setTimeout(function() {
  var streak = parseInt(document.getElementById('statStreak').textContent) || 0;
  countUp(document.getElementById('statStreak'), streak, '');
}, 50);
```

**2g.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 3: Commit**
```bash
git add index.html dashboard.html
git commit -m "feat: add bg system, particles, theme toggle, progress bar to login + dashboard"
```

---

## Task 3: session.html + test.html

**Files:** Modify `session.html`, `test.html`

- [ ] **Step 1: session.html**

**1a.** After `<body>`, add snippet A (page-bg). The session page has `.session-topbar` (sticky, not fixed). Ensure `position:relative;z-index:1` is on `.session-topbar` — already added in Task 1 CSS.

**1b.** Add theme toggle to session topbar. The session topbar has 3 cols: back | title | timer. Add the toggle inside the back column or as a 4th element:
Find:
```html
<a href="dashboard.html" class="topbar-back">
```
And after the close of the topbar, the topbar has `session-title` and `session-timer`. Add the theme button to the right side, replacing or alongside the timer if possible. Find the topbar structure in session.html and add:
```html
<div style="display:flex;align-items:center;gap:6px;min-width:60px;justify-content:flex-end;">
  <button class="topbar-icon" id="themeBtn" aria-label="Cambia tema" style="flex-shrink:0;"></button>
  <span class="session-timer" id="sessionTimer">0:00</span>
</div>
```
This groups theme toggle and timer together on the right.

**1c.** In the `<style>` block, add card lift animation for option buttons:
```css
.option-btn { transition: background 0.12s, border-color 0.12s, color 0.12s, transform 0.12s; }
.option-btn:hover:not(:disabled):not(.selected):not(.correct):not(.wrong) {
  transform: translateX(3px);
}
```

**1d.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 2: test.html**

**2a.** After `<body>`, add snippet A (page-bg).

**2b.** Add theme toggle to topbar (same as dashboard.html pattern — replace gear icon).

**2c.** Add `interactive` class to test selection cards. Find cards with class `.test-card` or similar — add `interactive` class to each. If cards use a different class, add the `card interactive` classes. Check actual class names in the file before editing.

**2d.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 3: Commit**
```bash
git add session.html test.html
git commit -m "feat: add bg, particles, theme toggle to session + test pages"
```

---

## Task 4: diagnostic.html + library.html

**Files:** Modify `diagnostic.html`, `library.html`

**CRITICAL for diagnostic.html:** Do NOT change any of these IDs or classes (they are used by `diagnostic.js`):
`headerTimer`, `headerTitle`, `progressFill`, `diagFooter`, `prevBtn`, `nextBtn`, `reviewBtn`, `backBtn`, `headerSubmitBtn`, `timeWarning`, `strat-*`, `q-card`, `opt-btn`, `opt-selected`, `passage-box`, `review-part-*`, `results-*`, `errors-*`, `phase`

- [ ] **Step 1: diagnostic.html**

**1a.** After `<body>`, add snippet A (page-bg).

**1b.** Add theme toggle to the existing 3-column header. The header has: left (back + title) | center (timer) | right (submit button). Add theme toggle in the left column alongside the back button:
```html
<!-- In the left column div, add after the back button -->
<button class="topbar-icon" id="themeBtn" aria-label="Cambia tema" style="flex-shrink:0;margin-left:4px;"></button>
```

**1c.** The progress bar in diagnostic uses `id="progressFill"` — do not rename. The existing progress bar element is fine as-is.

**1d.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 2: library.html**

**2a.** After `<body>`, add snippet A (page-bg).

**2b.** Add theme toggle to topbar (same pattern as dashboard.html).

**2c.** In the `<style>` block, enhance accordion hover:
```css
.accordion-header {
  transition: background 0.15s, padding-left 0.15s;
}
.accordion-header:hover {
  padding-left: 20px;
}
```

**2d.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 3: Commit**
```bash
git add diagnostic.html library.html
git commit -m "feat: add bg, particles, theme toggle to diagnostic + library"
```

---

## Task 5: stats.html + settings.html + log.html + review.html

**Files:** Modify `stats.html`, `settings.html`, `log.html`, `review.html`

- [ ] **Step 1: stats.html**

**1a.** After `<body>`, add snippet A (page-bg).

**1b.** Add theme toggle to topbar (same pattern as dashboard.html).

**1c.** Add `card-animate` + staggered delays to metric cards (first 4 stat cards):
```html
<div class="card stat-card card-animate" style="animation-delay:0.05s">
<div class="card stat-card card-animate" style="animation-delay:0.1s">
<div class="card stat-card card-animate" style="animation-delay:0.15s">
<div class="card stat-card card-animate" style="animation-delay:0.2s">
```

**1d.** Find the 4 metric `stat-num` elements and add count-up. In the inline `<script>` at bottom of stats.html, after stats are rendered, add:
```js
// Count-up on metric cards
function countUp(el, target, suffix) {
  if (!el || isNaN(target)) return;
  var step = target / 25; var cur = 0;
  (function tick() {
    cur = Math.min(cur + step, target);
    el.textContent = Math.round(cur) + (suffix || '');
    if (cur < target) requestAnimationFrame(tick);
  })();
}
setTimeout(function() {
  document.querySelectorAll('.stat-num').forEach(function(el) {
    var raw = parseFloat(el.textContent);
    var suffix = el.textContent.replace(/[\d.]/g, '');
    if (!isNaN(raw)) countUp(el, raw, suffix);
  });
}, 100);
```

**1e.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 2: settings.html**

**2a.** After `<body>`, add snippet A (page-bg).

**2b.** The theme toggle already exists in settings.html as a 3-button group. No topbar toggle needed here — settings is the dedicated page for it.

**2c.** Add snippet B (particles) before `</body>`. (No theme toggle JS needed — already implemented in settings.html.)

- [ ] **Step 3: log.html**

**3a.** After `<body>`, add snippet A (page-bg).

**3b.** Add theme toggle to topbar.

**3c.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 4: review.html**

**4a.** After `<body>`, add snippet A (page-bg).

**4b.** Add theme toggle to topbar.

**4c.** Add snippet B (particles) + snippet D (theme toggle JS) before `</body>`.

- [ ] **Step 5: Commit**
```bash
git add stats.html settings.html log.html review.html
git commit -m "feat: add bg, particles, theme toggle, count-up to stats/settings/log/review"
```

---

## Task 6: Verify + Push

- [ ] **Step 1: Smoke test** — Open `python3 -m http.server 8080` in the project dir. Visit:
  - `http://localhost:8080/index.html` — verify grid bg, glow, particles visible in dark mode
  - `http://localhost:8080/dashboard.html` — verify progress bar animates, stats count up, cards lift on hover
  - `http://localhost:8080/diagnostic.html` — verify no JS errors, all quiz functionality works
  - Toggle theme on each page — verify sun/moon icon switches, page re-themes correctly

- [ ] **Step 2: Check .gitignore for .superpowers**
```bash
cd "C:/Users/PC/Desktop/files/claude/tramitenglish"
grep -q ".superpowers" .gitignore 2>/dev/null || echo ".superpowers/" >> .gitignore
git add .gitignore
git commit -m "chore: add .superpowers to .gitignore"
```

- [ ] **Step 3: Push to GitHub**
```bash
git push origin main
```

Expected output: `main -> main` with no errors. Cloudflare Pages will auto-deploy.

---

## Self-Review Notes

- Spec requirement "grid + particles + progress bar" → Tasks 1 + 2 ✓
- Spec requirement "theme toggle accessible from every page" → Tasks 2–5 each add it ✓
- Spec requirement "animated interactive cards" → Task 1 CSS `.card.interactive` + per-page `interactive` class ✓
- Spec requirement "count-up on stats" → Tasks 2 + 5 ✓
- Spec requirement "preserve diagnostic.js contracts" → Task 4 explicitly lists forbidden IDs ✓
- Spec requirement "light mode works" → CSS handles both via data-theme + prefers-color-scheme ✓
- No placeholder code — all JS and CSS is complete and runnable ✓
